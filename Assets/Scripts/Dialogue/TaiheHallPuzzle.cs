using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TaiheHallPuzzle : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        CraftingSystem.Instance.CraftItem("taihe_hall_material");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
